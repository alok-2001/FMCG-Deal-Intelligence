# Nothing here is required to run the demo (data source = "Demo" in the app,
# or `mode="demo"` in pipeline/runner.py). The base pipeline never calls an
# LLM or a paid search API.
#
# Optional: only used by the disabled-by-default LLM refinement hook in
# pipeline/scoring.py (maybe_llm_refine), for nudging borderline relevance
# scores (40-60) once you wire up a model client there.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
